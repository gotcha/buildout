[buildout]
parts = demo
base-dir = ${buildout:directory}/var

[demo]
recipe = my.recipe
home = ${__environ__:HOME}
dollar = $$not-a-substitution
self = ${:recipe}
parent = ${buildout:base-dir}
installed = ${buildout:installed}
