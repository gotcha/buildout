# a comment
[buildout]
parts = demo
        extra
newest = false

[base]
<= parent
=> demo

[versions: python_version >= "3.13" and platform_system != "msdos"]
demo = 1.1 # trailing comment

[demo: sys.version_info[0] == 3]
recipe = zc.recipe.egg
eggs = demo ${buildout:directory} $$
       and ${demo:extra} on a continuation
