CI devenv shells no longer enable the Dagger/Podman services:
``services.dagger`` is disabled when the ``CI`` environment variable is
set, so CI legs skip the ``podman-machine:init`` shell-entry task
(85-120s each) and drop qemu/podman/dagger from the closure; local
shells keep the engine.  [gotcha]
