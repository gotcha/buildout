``etc/bump_ci_versions.py`` gains a test suite: ``dagger/tests/test_bump_ci_versions.py`` runs the bumper offline against a throwaway repo tree and pins every axis rule, the anchor-collision guards, partial-failure healing, and idempotent reruns; it ships in CI through the dagger ``module`` family cell that already runs the drift harness, and through the bumper's own final gate. [gotcha]
