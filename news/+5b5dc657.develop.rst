Re-select ruff ``F841`` and ``F811`` tree-wide. ``gen_pytest.py`` now computes the fixture unpacking set from the emitted test body instead of the doctest source, so regenerated ports stop over-unpacking; the five generated files shed 91 dead unpacking lines. Dead stores in doctest sources and their ports are gone (``_ =`` where the legacy doctest echo matters), along with three production dead stores. Skills gain the prose-preservation rule for port regeneration and the log-capture rule for legacy-suite runs. [gotcha]
