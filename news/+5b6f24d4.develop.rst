Toward full resolution hand-off with ``installer = uv``: the uv compile seam now accepts several requirements in one call (``_uv_resolve_requirements``), and the new ``install_pinned_dists`` installs a whole pinned set with a single ``uv pip install`` subprocess. Internal machinery only: the install loop still drives one requirement at a time, rewiring lands in a follow-up. [gotcha]
