Agent skills updated: ``verify-buildout`` documents the complexity
baseline's line pinning — any line-shifting edit, even comment-only,
drifts pinned entries, so ``make complexity`` rides in every
verification — and ``develop-buildout`` cross-references the rule.
[gotcha]
